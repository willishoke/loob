#include <iostream>
#include <vector>
#include <variant>
#include "loob.h"

int main(int argc, char** argv)
{
  Signal signal;
  /*
  signal = latch.process(Signal(Low()), Signal(High()));
  std::cout << "Low, High: " << signal.high() << std::endl;
  signal = latch.process(Signal(Low()), Signal(Low()));
  std::cout << "Low, Low: " << signal.high() << std::endl;
  signal = latch.process(Signal(High()), Signal(Low()));
  std::cout << "High, Low: " << signal.high() << std::endl;
  signal = latch.process(Signal(Low()), Signal(Low()));
  signal = latch.process(Signal(High()), Signal(High()));
  std::cout << "Low, Low: " << signal.high() << std::endl;
  */
  return 0;
}

